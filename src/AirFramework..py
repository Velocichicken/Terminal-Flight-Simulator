class aircraft:

    # class constructor
    def __init__(self, name, catagory, airframeRigidity, stallAOA, stallTAS, maxTAS, vsM, turnRate, flaps, acceleration, fuelCapacity, maxAltitude, fuelburnRate, maxFlaps):
      self.name = name
      self.catagory = catagory
      self.airframeRigidity = airframeRigidity
      self.stallAOA = stallAOA
      self.stallTAS = stallTAS
      self.maxTAS = maxTAS
      self.vsM = vsM
      self.turnRate = turnRate
      self.flaps = flaps
      self.acceleration = acceleration
      self.fuelCapacity = fuelCapacity
      self.maxAltitude = maxAltitude
      self.fuelBurnRate = fuelburnRate
      self.maxFlaps = maxFlaps


class airport:
   pass